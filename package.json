{
  "name": "fullstack-role-based-auth",
  "version": "1.0.0",
  "description": "Full-stack role-based login system with Node.js backend and React frontend following PDF structure guide",
  "scripts": {
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm run dev"
  }
}
